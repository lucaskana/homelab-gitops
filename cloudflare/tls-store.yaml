apiVersion: traefik.io/v1alpha1
kind: TLSStore
metadata:
  name: default
  namespace: ingress # Deve ser o mesmo namespace do Secret
spec:
  defaultCertificate:
    secretName: cloudflare-traefik-cert

